services:
  iris:
    build:
      dockerfile: Dockerfile
    restart: always
    command: --check-caps false --ISCAgent false
    ports:
      - 52773:52773
    volumes:
      - ./:/home/irisowner/dev
